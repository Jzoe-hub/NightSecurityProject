# ==============================================
# .gitignore — 告诉 Git 哪些文件不用管
# 每行一条规则，# 开头的是注释
# ==============================================

# --- 编译中间产物（每次编译都会变，没必要追踪）---
*.o         # C 编译生成的目标文件
*.axf       # Keil 链接输出文件
*.lst       # 汇编列表文件
*.map       # 内存映射文件
Objects/    # Keil 编译输出目录
Listings/   # Keil 列表文件目录

# --- 烧录文件（体积大，随时可重新编译生成）---
*.hex
*.bin

# --- Keil 用户个人配置（自己电脑的设置，不用共享）---
*.uvguix.*
*.uvoptx

# --- 可执行文件和库（工具软件，非源码）---
*.exe
*.dll

# --- 指纹测试工具（含生物特征数据，不能上传）---
sources/ZW模块/ZWDemo_2022080316测试工具/

# --- 压缩包 ---
*.rar
*.zip
*.7z

# --- 各种编辑器的临时文件 ---
*.swp       # Vim
*.bak       # 备份文件
*.tmp       # 临时文件
~*          # 某些编辑器的备份前缀
